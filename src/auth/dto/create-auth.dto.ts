/**
 * Placeholder DTO scaffolded for a future "create auth" endpoint.
 *
 * Intentionally empty and unused - no route references it. Kept only so the scaffolded
 * {@link UpdateAuthDto} still has a base to extend.
 */
export class CreateAuthDto {}
